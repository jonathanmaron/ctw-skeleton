<?php
declare(strict_types=1);

namespace Ctw\Skeleton;

/**
 * Placeholder class for the ctw-skeleton template.
 *
 * ctw-skeleton ships without real source classes. This placeholder gives the
 * PHPUnit code-coverage source filter (the `<source>` include in
 * phpunit.xml.dist, which only matches `*.php`) a file to match, so
 * `composer test` runs clean instead of failing with "Configured source filter
 * does not match any files". Replace it with real classes when building on the
 * skeleton.
 */
final class Skeleton
{
}
